# Vendored SDKs (license-gated downloads) — not tracked yet.
# Will likely be added once the full offline build package is set up
# for Releases; leave this note until that's decided.
Ultralight SDK/
XPSDK4.3.0/

# Design exports (Claude Design mockups — not for public repo)
xefb/design-doc/

# Build output — ignore everything in xefb/build/ EXCEPT the packaged
# drop-in plugin folder (xefb/build/xEFB/), which is committed on
# purpose so it can be copied straight into X-Plane without rebuilding.
xefb/build/*
!xefb/build/xEFB
!xefb/build/xEFB/**

out/
cmake-build-*/
*.obj
*.pdb

# Editor / OS cruft
.vs/
.vscode/
*.user
.DS_Store
Thumbs.db
CMakeUserPresets.json

# Ultralight runtime cache
cache/
resources/cache/

# Claude Code handoff notes (internal AI-assist scratch docs, not for the public repo)
CLAUDE_CODE_HANDOFF.md
xefb/CLAUDE_CODE_HANDOFF.md
